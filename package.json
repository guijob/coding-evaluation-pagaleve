{
  "name": "coding-evaluation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently -n webpack,sam \"webpack -w\" \"sam local start-api --docker-network lambda-local --env-vars test/resources/env.dev.json\"",
    "test": "jest --runInBand",
    "test:ci": "jest --runInBand",
    "test:watch": "jest --watch",
    "clean": "rm -rf -v dist/*",
    "build": "NODE_ENV=${NODE_ENV:-production} webpack-cli",
    "package": "aws cloudformation package --template-file template.yaml --output-template-file template.packaged.yaml --s3-bucket coding-evaluation-pagaleve-back-${STAGE:-dev}-app",
    "deploy": "aws cloudformation deploy --template-file template.packaged.yaml --stack-name coding-evaluation-pagaleve-back-${STAGE:-dev} --capabilities CAPABILITY_IAM --parameter-overrides Environment=${STAGE:-dev} SamLocalURL=${SamLocalURL:-DISABLED} --no-fail-on-empty-changeset"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.5.0",
    "chai": "^4.3.6",
    "ts-loader": "^9.3.0",
    "webpack": "^5.72.0",
    "webpack-cli": "^4.9.2",
    "yaml-cfn": "^0.3.1"
  },
  "dependencies": {
    "jest": "^28.1.0"
  }
}
